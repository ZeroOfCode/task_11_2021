#ifndef TASK_H
#define TASK_H
#include <iostream>
#include <iomanip>

bool find_way(size_t *array, size_t x, size_t y, size_t point_x, size_t point_y);
bool find_true_way(size_t *array, size_t x, size_t y, bool show = false);

#endif